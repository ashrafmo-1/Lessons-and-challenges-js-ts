// Reamove last element from the array
let fruits = ['apple', 'banana', 'orange'];
console.log(fruits); // ['apple', 'banana', 'orange']
console.log(fruits.pop()); // lastFruit is 'orange'
console.log(fruits); // ['apple', 'banana']

// Reamove First element from the array
let reada = ['taira', 'cora', 'basket'];
console.log(reada);
console.log(reada.shift());
console.log(reada);